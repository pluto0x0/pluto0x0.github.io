---
title: ECE391 Course Notes
date: 2023-11-08 20:41:49
math: true
img_path: /_posts/
---

## 0 intro

fuck you ans kiss my ass.

## 9 Programmable interrupt controller (PIC)

### Motivation

Connect (more then 1)    devices to processor's interrupt input.

> Why not use a OR gate?
>
> no way to tell which device
{: .prompt-tip }

### Overview

![PIC](../upload/img/2023-11-08-ECE391-image.png)

- `A` = address match for ports
- `CS’` = chip select (does processor want PIC to read/write?)
- `RD’` and `WR’` defined from processor’s point of view
  - `RD’` = processor will read data (vector #) from PIC
  - `WR’` = processor will write data (command, EOI) to PIC

Ports: `0x20` & `0x21`

![prot](../upload/img/2023-11-08-ECE391-image-1.png)

i.e. `CS'` only enabled when `ADDR` is `0x20` or `0x21`.


### Steps

When a device **raises an interrupt**...

- check if priority is higher than those of in service interrupts
- if not, do nothing
- if so
  - report the highest priority raised to the processor
  - mark that device as being in service

When processor reports `EOI` (end of interrupt) for
some interrupt...

- remove the interrupt from the in service mask
- check for raised interrupt lines that should be reported to processor

How to report a interrupt?

- PIC raises `INTR`
- processor strobes `INTA’` (active low) repeatedly
  - creates cycles for PIC to write **vector** to data bus
  - (must follow spec timing! PIC is not infinitely fast.)
- processor sends `EOI` with specific combinations of A & D inputs (A is from address bus, D is from data bus)

<!-- 1. **IRQ Activation**: An IRQ line is activated by a device that requires the CPU's attention.

2. **PIC Acknowledgment**: The 8259 PIC acknowledges the IRQ and sends an interrupt request to the CPU's **INTR** (interrupt request) line.

3. **CPU Response**: Upon receiving the interrupt request on the INTR line, the CPU finishes the current instruction and sends an interrupt acknowledge signal (**INTA'**) back to the PIC.

4. **Interrupt Vector**: The PIC then responds to the INTA by placing an interrupt vector on the data bus. This vector is an address pointing to the specific location in the Interrupt Vector Table (IVT) where the Interrupt Service Routine (ISR) for the interrupting device is located.

5. **ISR Execution**: The CPU uses the interrupt vector to fetch and execute the appropriate ISR from the IVT.

6. **End of Interrupt (EOI)**: Once the ISR is completed, the PIC must be sent an EOI signal to inform it that the interrupt has been serviced and that the PIC can resume sending interrupt requests to the CPU.

This is a simplified overview of the process. The actual implementation can vary depending on whether the system is using the 8259 PIC in a single or cascaded configuration, and whether it is set up for automatic EOI or requires a specific EOI command. -->